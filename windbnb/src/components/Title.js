import React from "react";
import PropTypes from "prop-types";

const Title = (props) => {
  return (
    <div>
      Title<span>aa</span>
    </div>
  );
};

Title.propTypes = {};

export default Title;
