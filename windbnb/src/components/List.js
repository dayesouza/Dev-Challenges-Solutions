import React from "react";
import PropTypes from "prop-types";

const List = (props) => {
  return (
    <div>
      list<span>aa</span>
    </div>
  );
};

List.propTypes = {};

export default List;
