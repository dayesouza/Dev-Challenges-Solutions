import React from "react";
import PropTypes from "prop-types";

const TopBar = (props) => {
  return (
    <div>
      TopBar<span>aa</span>
    </div>
  );
};

TopBar.propTypes = {};

export default TopBar;
