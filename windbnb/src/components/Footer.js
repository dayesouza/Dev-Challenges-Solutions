import React from "react";
import PropTypes from "prop-types";

const Footer = (props) => {
  return (
    <div>
      Footer<span>aa</span>
    </div>
  );
};

Footer.propTypes = {};

export default Footer;
